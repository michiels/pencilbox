<article>
  <% markdown = Redcarpet::Markdown.new(HTMLWithAlbino, :fenced_code_blocks => true) %>
  <%= raw(markdown.render(article.body).gsub(/src=\"(.+\.(png|jpg))\"/) { "src=\"#{box_path(@user.username)}/files/#{$1}\""}) %>
</article>