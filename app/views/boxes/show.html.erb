
<% @articles.each do |article| %>
  <article>
    <% markdown = Redcarpet::Markdown.new(HTMLWithAlbino, :fenced_code_blocks => true) %>
    <%= raw(markdown.render(article.body).gsub(/images\//, "#{box_path(@box)}/images/")) %>
  </article>
<% end %>